using Content.Shared._Redshift.Sex;

namespace Content.Client._Redshift.Sex;

// empty, just here to make sure shared->shared ArousalSystem dependencies don't fall through
public sealed class ArousalSystem : SharedArousalSystem { }
